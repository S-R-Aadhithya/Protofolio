image: python:3.11-slim

stages:
  - build
  - test
  - deploy

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  key:
    files:
      - requirements.txt
  paths:
    - .cache/pip/

install-deps:
  stage: build
  script:
    - pip install --upgrade pip
    - pip install -r requirements.txt mlflow optuna



unit-tests:
  stage: test
  script:
    - pip install -r requirements.txt pytest pytest-cov mlflow optuna
    - pytest tests/unit/ -v --tb=short --cov=app --cov-report=term-missing
  coverage: '/TOTAL.*\s+(\d+%)$/'

integration-tests:
  stage: test
  script:
    - pip install -r requirements.txt pytest mlflow optuna
    - pytest tests/integration/ -v --tb=short

run-experiments:
  stage: test
  script:
    - pip install -r requirements.txt mlflow optuna
    - python run_experiments.py
  allow_failure: true
  artifacts:
    paths:
      - mlruns/

run-optuna-tuning:
  stage: test
  script:
    - pip install -r requirements.txt mlflow optuna
    - python optuna_tuner.py --trials 5
  allow_failure: true
  artifacts:
    paths:
      - mlruns/
  variables:
    GIT_PYTHON_REFRESH: quiet

deploy-staging:
  stage: deploy
  script:
    - apt-get update -qq && apt-get install -y curl
    - echo "Deploying to https://protofolio-backend.onrender.com"
    - curl --max-time 60 https://protofolio-backend.onrender.com/ && echo "Deployment live" || echo "Service warming up on Render free tier"
  allow_failure: true
  only:
    - develop

deploy-production:
  stage: deploy
  script:
    - apt-get update -qq && apt-get install -y curl
    - echo "Deploying to https://protofolio-backend.onrender.com"
    - curl --max-time 60 https://protofolio-backend.onrender.com/ && echo "Production live" || echo "Service warming up on Render free tier"
  allow_failure: true
  only:
    - main
  when: manual
